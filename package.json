{
  "name": "redux-constants-builder",
  "version": "2.0.2",
  "description": "Small package to reduce the boilerplate when writing redux constants",
  "repository": {
    "type": "git",
    "url": "https://github.com/stjepangolemac/redux-constants-builder"
  },
  "main": "dist/index.js",
  "types": "src/index",
  "files": [
    "dist/"
  ],
  "scripts": {
    "test": "mocha dist/test/*.test.js",
    "build": "tsc",
    "dev": "npm run build && npm test",
    "watch": "watch 'npm run dev' src",
    "preversion": "npm run build && npm test"
  },
  "keywords": [
    "redux",
    "constants",
    "builder",
    "reduce",
    "boilerplate"
  ],
  "author": "Stjepan Golemac",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^3.5.1",
    "@types/mocha": "^2.2.41",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "typescript": "^2.2.2",
    "watch": "^1.0.2"
  }
}
